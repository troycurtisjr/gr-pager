# auto-generated by grc.converter

id: pager_flex_deinterleave
label: FLEX Deinterleave
category: '[Pager]'

inputs:
-   label: bits
    domain: stream
    dtype: byte

outputs:
-   label: codes
    domain: stream
    dtype: int

templates:
    imports: import gnuradio.pager as pager
    make: pager.flex_deinterleave()

file_format: 1
