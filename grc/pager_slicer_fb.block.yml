# auto-generated by grc.converter

id: pager_slicer_fb
label: 4-Level Slicer/DCR
category: '[Pager]'

parameters:
-   id: alpha
    label: Alpha
    dtype: real
    default: 1e-6

inputs:
-   label: bb
    domain: stream
    dtype: float

outputs:
-   label: dibits
    domain: stream
    dtype: byte

templates:
    imports: import pager
    make: pager.slicer_fb(${alpha})

file_format: 1
